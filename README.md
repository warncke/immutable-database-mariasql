# immutable-database-mariasql

Database driver designed for use with immutable-core.

## Native async/await

Immutable Database MariaSQL requires Node.js v7.6.0 or greater with native
async/await support.